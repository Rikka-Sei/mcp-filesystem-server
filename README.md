# mcp-filesystem-server

A basic Model Context Protocol (MCP) filesystem server implementation using SSE (Server-Sent Events).

## Getting Started

1. Install dependencies:
   ```bash
   npm install
   ```

2. Start the server:
   ```bash
   npm start
   ```
